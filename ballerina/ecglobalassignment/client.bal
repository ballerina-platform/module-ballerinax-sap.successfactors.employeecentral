// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2026, WSO2 LLC. (http://www.wso2.org).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/data.jsondata;
import ballerina/http;
import ballerinax/sap;

# You can use these APIs to manage the assignment information of the employees who are assigned to another company within the organization.
# 
#  You can find your company's API server in [List of API Servers in SAP SuccessFactors.](https://help.sap.com/viewer/d599f15995d348a1b45ba5603e2aba9b/LATEST/en-US/af2b8d5437494b12be88fe374eba75b6.html)
public isolated client class Client {
    final sap:Client clientEp;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string hostname, int port = 443) returns error? {
string serviceUrl = string `https://${hostname}:${port}/successfactors/odata/v2`;
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # Get entities from SecondaryAssignmentsItem
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved entities 
    remote isolated function listSecondaryAssignmentsItems(map<string|string[]> headers = {}, *ListSecondaryAssignmentsItemsQueries queries) returns Wrapper|error {
        string resourcePath = string `/SecondaryAssignmentsItem`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Add new entity to SecondaryAssignmentsItem
    #
    # + headers - Headers to be sent with the request 
    # + payload - New entity 
    # + return - Created entity 
    remote isolated function createSecondaryAssignmentsItem(SecondaryAssignmentsItem payload, map<string|string[]> headers = {}) returns CreatedSecondaryAssignmentsItem|error {
        string resourcePath = string `/SecondaryAssignmentsItem`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get entity from SecondaryAssignmentsItem by key
    #
    # + SecondaryAssignments_effectiveStartDate - key: SecondaryAssignments_effectiveStartDate
    # + SecondaryAssignments_externalCode - key: SecondaryAssignments_externalCode
    # + externalCode - key: externalCode
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved entity 
    remote isolated function getSecondaryAssignmentsItem(string SecondaryAssignments_effectiveStartDate, string SecondaryAssignments_externalCode, string externalCode, map<string|string[]> headers = {}, *GetSecondaryAssignmentsItemQueries queries) returns SecondaryAssignmentsItem|error {
        string resourcePath = string `/SecondaryAssignmentsItem(SecondaryAssignments_effectiveStartDate=${getEncodedUri(SecondaryAssignments_effectiveStartDate)},SecondaryAssignments_externalCode='${getEncodedUri(SecondaryAssignments_externalCode)}',externalCode='${getEncodedUri(externalCode)}')`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update entity in SecondaryAssignmentsItem
    #
    # + SecondaryAssignments_effectiveStartDate - key: SecondaryAssignments_effectiveStartDate
    # + SecondaryAssignments_externalCode - key: SecondaryAssignments_externalCode
    # + externalCode - key: externalCode
    # + headers - Headers to be sent with the request 
    # + payload - New property values 
    # + return - Success 
    remote isolated function updateSecondaryAssignmentsItem(string SecondaryAssignments_effectiveStartDate, string SecondaryAssignments_externalCode, string externalCode, ModifiedSecondaryAssignmentsItem payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/SecondaryAssignmentsItem(SecondaryAssignments_effectiveStartDate=${getEncodedUri(SecondaryAssignments_effectiveStartDate)},SecondaryAssignments_externalCode='${getEncodedUri(SecondaryAssignments_externalCode)}',externalCode='${getEncodedUri(externalCode)}')`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete entity from SecondaryAssignmentsItem
    #
    # + SecondaryAssignments_effectiveStartDate - key: SecondaryAssignments_effectiveStartDate
    # + SecondaryAssignments_externalCode - key: SecondaryAssignments_externalCode
    # + externalCode - key: externalCode
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteSecondaryAssignmentsItem(string SecondaryAssignments_effectiveStartDate, string SecondaryAssignments_externalCode, string externalCode, DeleteSecondaryAssignmentsItemHeaders headers = {}) returns error? {
        string resourcePath = string `/SecondaryAssignmentsItem(SecondaryAssignments_effectiveStartDate=${getEncodedUri(SecondaryAssignments_effectiveStartDate)},SecondaryAssignments_externalCode='${getEncodedUri(SecondaryAssignments_externalCode)}',externalCode='${getEncodedUri(externalCode)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get entities from SecondaryAssignments
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved entities 
    remote isolated function listSecondaryAssignmentss(map<string|string[]> headers = {}, *ListSecondaryAssignmentssQueries queries) returns Wrapper_1|error {
        string resourcePath = string `/SecondaryAssignments`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Add new entity to SecondaryAssignments
    #
    # + headers - Headers to be sent with the request 
    # + payload - New entity 
    # + return - Created entity 
    remote isolated function createSecondaryAssignments(SecondaryAssignments payload, map<string|string[]> headers = {}) returns CreatedSecondaryAssignments|error {
        string resourcePath = string `/SecondaryAssignments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get entity from SecondaryAssignments by key
    #
    # + effectiveStartDate - key: effectiveStartDate
    # + externalCode - key: externalCode
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved entity 
    remote isolated function getSecondaryAssignments(string effectiveStartDate, string externalCode, map<string|string[]> headers = {}, *GetSecondaryAssignmentsQueries queries) returns SecondaryAssignments|error {
        string resourcePath = string `/SecondaryAssignments(effectiveStartDate=${getEncodedUri(effectiveStartDate)},externalCode='${getEncodedUri(externalCode)}')`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update entity in SecondaryAssignments
    #
    # + effectiveStartDate - key: effectiveStartDate
    # + externalCode - key: externalCode
    # + headers - Headers to be sent with the request 
    # + payload - New property values 
    # + return - Success 
    remote isolated function updateSecondaryAssignments(string effectiveStartDate, string externalCode, ModifiedSecondaryAssignments payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/SecondaryAssignments(effectiveStartDate=${getEncodedUri(effectiveStartDate)},externalCode='${getEncodedUri(externalCode)}')`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete entity from SecondaryAssignments
    #
    # + effectiveStartDate - key: effectiveStartDate
    # + externalCode - key: externalCode
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteSecondaryAssignments(string effectiveStartDate, string externalCode, DeleteSecondaryAssignmentsHeaders headers = {}) returns error? {
        string resourcePath = string `/SecondaryAssignments(effectiveStartDate=${getEncodedUri(effectiveStartDate)},externalCode='${getEncodedUri(externalCode)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get entities from RightToReturn
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved entities 
    remote isolated function listRightToReturns(map<string|string[]> headers = {}, *ListRightToReturnsQueries queries) returns Wrapper_2|error {
        string resourcePath = string `/RightToReturn`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Add new entity to RightToReturn
    #
    # + headers - Headers to be sent with the request 
    # + payload - New entity 
    # + return - Created entity 
    remote isolated function createRightToReturn(RightToReturn payload, map<string|string[]> headers = {}) returns CreatedRightToReturn|error {
        string resourcePath = string `/RightToReturn`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get entity from RightToReturn by key
    #
    # + Position_code - key: Position_code
    # + Position_effectiveStartDate - key: Position_effectiveStartDate
    # + code - key: code
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved entity 
    remote isolated function getRightToReturn(string Position_code, string Position_effectiveStartDate, string code, map<string|string[]> headers = {}, *GetRightToReturnQueries queries) returns RightToReturn|error {
        string resourcePath = string `/RightToReturn(Position_code='${getEncodedUri(Position_code)}',Position_effectiveStartDate=${getEncodedUri(Position_effectiveStartDate)},code='${getEncodedUri(code)}')`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update entity in RightToReturn
    #
    # + Position_code - key: Position_code
    # + Position_effectiveStartDate - key: Position_effectiveStartDate
    # + code - key: code
    # + headers - Headers to be sent with the request 
    # + payload - New property values 
    # + return - Success 
    remote isolated function updateRightToReturn(string Position_code, string Position_effectiveStartDate, string code, ModifiedRightToReturn payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/RightToReturn(Position_code='${getEncodedUri(Position_code)}',Position_effectiveStartDate=${getEncodedUri(Position_effectiveStartDate)},code='${getEncodedUri(code)}')`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete entity from RightToReturn
    #
    # + Position_code - key: Position_code
    # + Position_effectiveStartDate - key: Position_effectiveStartDate
    # + code - key: code
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteRightToReturn(string Position_code, string Position_effectiveStartDate, string code, DeleteRightToReturnHeaders headers = {}) returns error? {
        string resourcePath = string `/RightToReturn(Position_code='${getEncodedUri(Position_code)}',Position_effectiveStartDate=${getEncodedUri(Position_effectiveStartDate)},code='${getEncodedUri(code)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get entities from EmpGlobalAssignment
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved entities 
    remote isolated function listEmpGlobalAssignments(map<string|string[]> headers = {}, *ListEmpGlobalAssignmentsQueries queries) returns Wrapper_3|error {
        string resourcePath = string `/EmpGlobalAssignment`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get entity from EmpGlobalAssignment by key
    #
    # + userId - key: userId
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved entity 
    remote isolated function getEmpGlobalAssignment(string userId, map<string|string[]> headers = {}, *GetEmpGlobalAssignmentQueries queries) returns EmpGlobalAssignment|error {
        string resourcePath = string `/EmpGlobalAssignment('${getEncodedUri(userId)}')`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }
}
